在C语言中，程序员要手动分配内存，这样可以节省大量的内存空间，让程序员更加灵活。只要有一定的基础，那么肯定可以malloc和free的组合。

所指向的内存未申请
所指向的内存被释放

指针声明未被初始化
指针指向的空间被free后，没有重置为NULL，自己要注意避免。

野指针避免应该操作如下：
int *p = NULL；//默认将其初始化为NULL

在free后面,加上重置指针的代码:
free(p);
p = NULL;

